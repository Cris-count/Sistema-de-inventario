package com.inventario.domain.entity;

public enum SaasCompraTipo {
    /** Alta inicial (onboarding con pago pendiente). */
    ONBOARDING,
    /** Cambio de plan con pago pendiente (upgrade). */
    CAMBIO_PLAN
}
