# Copiar a `.env` en la raíz del repo (opcional) para sobrescribir valores de docker compose.
# Ver README: sección Docker y variables del backend.

JWT_SECRET=
APP_SEED_ADMIN_EMAIL=
APP_SEED_ADMIN_PASSWORD=
