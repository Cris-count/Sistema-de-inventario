/**
 * Archivos en `public/branding/`. Mantener nombres estables; favicon en `index.html` debe coincidir.
 * Si más adelante hay un ícono cuadrado solo-marca, sustituir `mark` y dejar `logoFull` para navbar/hero.
 */
export const BRANDING_ASSETS = {
  logoFull: '/branding/cersik-logo-full.png',
  mark: '/branding/cersik-mark.png',
} as const;
